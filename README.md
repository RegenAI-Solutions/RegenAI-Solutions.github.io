# RegenAI-Solutions.github.io

tôi đã mua tên miền là regenaisolutions.com rồi, làm sao để push cái này lên tên miền ấy

oke, trong lúc chờ DNS, lên kế hoạch để implement 1 phần login cơ bản để manage api-key cho user
hiện tại tôi đã tạo ra phần auth ở source daycent-gateway, bạn check qua, và tạo cho tôi 1 UI cơ bản giúp:
- admin quản lý user
- user đăng nhập để tạo api-key, sử dụng daycent services

tôi login được rồi, nhưng các api khác vẫn bị lỗi (ví dụ api-keys, create user,...)

còn về phía admin, trong users vẫn chưa list ra các users để quản lý

oke, đã hoạt động, giờ tôi cần có 1 button để đăng nhập vào trên giao diện ở index.html ấy